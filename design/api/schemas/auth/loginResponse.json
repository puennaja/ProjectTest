{
  "type": "object",
  "required": ["access_token", "refresh_token"],
  "description": "response token object from the server",
  "properties": {
    "access_token": {
      "type": "string"
    },
    "refresh_token": {
      "type": "string"
    }
  }
}
